function outvar = scheck_model_scanpaths(model,cfg)
% Sanity check for model's scanpaths

% has forced first fixation


% first value is x and second value is y and fall inside video


% scanpath size


% if found check that the model has seen in the last fixation the target


% if no found check that the model hast seen the target

outputArg1 = inputArg1;
outputArg2 = inputArg2;
end

